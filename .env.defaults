// Defaults for development
// over-ride these values in your own .env file
// Set testing specified values in a .env.testing file

//  MongoDB host and database name
DB_URL=mongodb://localhost:27017/
DB_NAME=test1